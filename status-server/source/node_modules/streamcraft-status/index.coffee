async= require 'async'
fs= require 'fs'
mcquery= require 'mcquery'

class Status
    @intervalID: {}



    @check: (config, output, done) ->

        if not fs.existsSync config
            return done() if done
            return


        servers= JSON.parse fs.readFileSync config

        # итерация по каждому серверу в массиве
        async.map servers
        ,   (server, cb) ->
            status= {}

            status=
                title: server.title
                host: server.host
                port: server.port

            query= new mcquery server.host, server.port

            # cоединяемся
            query.connect (err) ->
                if err
                    do query.close
                    status.error= err.error
                    return cb null, output
                # если соединилсь успешно, берем статистику
                query.basic_stat (err, stat) ->
                    do query.close

                    if err
                        status.error= err.error
                    else
                        status.stat=
                            numplayers: stat.numplayers
                            maxplayers: stat.maxplayers

                    return cb null, status

        ,   (err, result) ->
            if done
                return done result
            else
                fs.writeFile output, JSON.stringify(result, null, 4), (err) ->
                    return console.log err if err



    @start: (config, output, sec) ->

        intervalFunction= => @check config, output
        @intervalID= setInterval intervalFunction, sec * 1000

        return @intervalID



    @stop: ->
        clearInterval @intervalID



    @isWorking: ->
        if @intervalID.ontimeout instanceof Function
            return true
        else
            return false



module.exports= Status

